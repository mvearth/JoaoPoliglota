# Copy to .env and adjust. Do not commit your real .env.

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=joaopoliglota
DB_SSLMODE=disable

# HTTP server listen address
HTTP_ADDR=:8000

# Path to the Google Cloud service-account key used by the Translation API
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
